// Type definition for teachers
export type TeacherOption = {
  value: string;
  label: string;
};

// Define type for Departments
export type Departments = {
  value: string;
};

// Defines types for form data
export type FormData = {
  department: string;
  head: string;
};
